import { Registration } from '@mcp3/common';

/**
 * Register balance-related tools with the Registration
 * @param registration The Registration instance
 */
export function registerBalanceTools(registration: Registration) {
  // Balance tools have been removed as requested
}
