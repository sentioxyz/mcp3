{
  "name": "mcp3",
  "version": "1.0.0",
  "description": "Web3 Model Context Protocol Server",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test"
  },
  "keywords": [
    "blockchain",
    "mcp",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "devDependencies": {
    "glob": "^11.0.1",
    "typescript": "^5.2.2",
    "tsx": "^4.15.2"
  }
}
